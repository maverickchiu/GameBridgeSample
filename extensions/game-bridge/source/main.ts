/**
 * Game Bridge extension entry (Cocos Creator editor extension).
 * Export your extension logic from here.
 */
export function load(): void {
  // Extension load hook
}

export function unload(): void {
  // Extension unload hook
}
